# Moon Light Frontend

## Startup

1. Install dependencies:
```powershell
npm install
```

2. Start development server:
```powershell
npm run dev
```

Vite will run the frontend locally.

## Other Scripts

- Build: `npm run build`
- Preview production build: `npm run preview`
- Lint: `npm run lint`
